import {COLORS} from '../../constants';
import {BeatLoader} from 'react-spinners';
import React from 'react';

const ProgressIndicator = () => <BeatLoader size={12} color={COLORS.theme} />;

export default ProgressIndicator;
